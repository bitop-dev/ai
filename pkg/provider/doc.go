// Package provider defines the interfaces that provider implementations must satisfy.
//
// Provider authors should implement the model interfaces in this package and expose
// a ProviderV3 that hands back the models they support.
package provider
