# Copy to .env and fill values. Do NOT commit .env.
OPENAI_API_KEY=your_key_here
OPENAI_MODEL=gpt-4.1
AI_INTEGRATION=1
# For ./examples/multimodal_image:
# IMAGE_URL=https://upload.wikimedia.org/wikipedia/commons/thumb/3/3a/Cat03.jpg/640px-Cat03.jpg
# For ./examples/agent_steps:
# PROMPT=Use the add tool to add 1 and 2, then reply with just the final number.
# For ./examples/agent_loop_controls:
# PROMPT=Find two examples of Go web frameworks, then summarize.
# For ./examples/stream_text_tool_hooks:
# PROMPT=Use the add tool to add 123 and 456. Then respond with just the final number.
# For ./examples/mcp_tools:
# MCP_URL=http://localhost:8080/mcp
# OPENAI_BASE_URL=https://api.openai.com
# OPENAI_API_PREFIX=/v1
